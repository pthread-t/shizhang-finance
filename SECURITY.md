# 安全策略

## 报告漏洞

请通过 GitHub 的私密漏洞报告功能提交安全问题，不要创建公开 Issue。报告中请写明受影响版本、复现步骤、可能影响和建议修复方向。

不要在报告中附带真实账单、账号密码、访问令牌、恢复码或服务器备份。如果复现需要数据，请使用仓库中的虚构 fixture 或自行构造最小样本。

## 维护范围

安全修复以 `main` 分支为准。旧安装包和自行修改的部署环境不承诺单独维护，但能够在当前代码中复现的问题仍欢迎报告。

服务端不是端到端加密设计。服务器管理员可以读取账本数据，部署者需要自行负责 TLS、主机权限、磁盘加密、安全更新和异地备份。
